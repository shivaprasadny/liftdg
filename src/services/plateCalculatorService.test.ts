import{describe,expect,it}from'vitest';import{calculatePlateLoad,generateWarmups,percentageLoad}from'./plateCalculatorService';
const plates=[45,35,25,10,5,2.5].map(plateWeight=>({plateWeight,quantity:4,enabled:true}));
describe('plate calculator',()=>{it('loads 185 lb exactly',()=>expect(calculatePlateLoad({targetWeight:185,barWeight:45,collarWeight:0,plates,mode:'closest'})).toMatchObject({loadedWeight:185,platesPerSide:[45,25],exact:true}));it('loads 100 kg exactly',()=>expect(calculatePlateLoad({targetWeight:100,barWeight:20,collarWeight:0,plates:[{plateWeight:20,quantity:4,enabled:true}],mode:'exact'}).platesPerSide).toEqual([20,20]));it('includes total collar weight once',()=>expect(calculatePlateLoad({targetWeight:190,barWeight:45,collarWeight:5,plates,mode:'exact'}).loadedWeight).toBe(190));it('rejects target below base',()=>expect(()=>calculatePlateLoad({targetWeight:40,barWeight:45,collarWeight:0,plates,mode:'closest'})).toThrow(/lower/));it('respects individual inventory quantities as pairs',()=>expect(()=>calculatePlateLoad({targetWeight:225,barWeight:45,collarWeight:0,plates:[{plateWeight:45,quantity:2,enabled:true}],mode:'exact'})).toThrow(/exact/));it('finds closest below and above',()=>{expect(calculatePlateLoad({targetWeight:188,barWeight:45,collarWeight:0,plates,mode:'below'}).loadedWeight).toBe(185);expect(calculatePlateLoad({targetWeight:188,barWeight:45,collarWeight:0,plates,mode:'above'}).loadedWeight).toBeGreaterThan(188)});it('is deterministic and prefers fewer plates',()=>expect(calculatePlateLoad({targetWeight:185,barWeight:45,collarWeight:0,plates,mode:'exact'}).platesPerSide).toEqual([45,25]));it('enforces bar minimum in warmups',()=>expect(generateWarmups({targetWeight:100,barWeight:45,collarWeight:0,plates,mode:'closest'},'simple')[0].load.loadedWeight).toBe(45));it('calculates loadable backoff percentage',()=>expect(percentageLoad({targetWeight:225,barWeight:45,collarWeight:0,plates,mode:'closest'},90).loadedWeight).toBeGreaterThanOrEqual(195));});
