import { ScrollView, StyleSheet } from 'react-native'; import { FilterChip } from './FilterChip'; import { cardioActivityTypes, type CardioActivityType } from '@/types/cardio'; import { spacing } from '@/constants/spacing';
export function CardioActivityPicker({value,onChange}:{value:CardioActivityType;onChange:(value:CardioActivityType)=>void}){return <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.row}>{cardioActivityTypes.map(item=><FilterChip key={item} label={item.replaceAll('_',' ')} selected={value===item} onPress={()=>onChange(item)}/>)}</ScrollView>;}const styles=StyleSheet.create({row:{gap:spacing.sm}});
