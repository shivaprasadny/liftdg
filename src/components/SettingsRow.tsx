import type { ReactNode } from 'react'; import { Pressable,StyleSheet,Text,View } from 'react-native'; import { Ionicons } from '@expo/vector-icons';
import { colors } from '@/constants/colors'; import { spacing } from '@/constants/spacing'; import { typography } from '@/constants/typography';
export function SettingsRow({label,value,onPress,control,danger=false}: {label:string;value?:string;onPress?:()=>void;control?:ReactNode;danger?:boolean}){return <Pressable disabled={!onPress} onPress={onPress} style={styles.row} accessibilityRole={onPress?'button':undefined}><View style={styles.text}><Text style={[styles.label,danger&&styles.danger]}>{label}</Text>{value?<Text style={styles.value}>{value}</Text>:null}</View>{control??(onPress?<Ionicons name="chevron-forward" size={20} color={colors.textMuted}/>:null)}</Pressable>;}const styles=StyleSheet.create({row:{minHeight:54,paddingHorizontal:spacing.md,paddingVertical:spacing.sm,borderBottomWidth:StyleSheet.hairlineWidth,borderBottomColor:colors.border,flexDirection:'row',alignItems:'center',gap:spacing.sm},text:{flex:1},label:{...typography.body,color:colors.text},value:{...typography.caption,color:colors.textMuted,marginTop:2},danger:{color:colors.danger}});

