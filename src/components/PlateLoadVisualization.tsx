import{StyleSheet,Text,View}from'react-native';import{colors}from'@/constants/colors';import{radius,spacing}from'@/constants/spacing';import{typography}from'@/constants/typography';import type{PlateUnit}from'@/types/plateCalculator';
export function PlateLoadVisualization({plates,unit,total}:{plates:number[];unit:PlateUnit;total:number}){return <View accessibilityLabel={`Each side uses ${plates.length?plates.map(x=>`${x} ${unit}`).join(', '):'no plates'}. Total loaded weight is ${total} ${unit}.`} style={s.wrap}><Text style={s.label}>BAR</Text><View style={s.sleeve}/>{plates.map((p,i)=><View key={`${p}-${i}`} style={[s.plate,{height:48+Math.min(38,p)}]}><Text style={s.text}>{p}</Text></View>)}<Text style={s.collar}>COLLAR</Text></View>}
const s=StyleSheet.create({wrap:{minHeight:100,flexDirection:'row',alignItems:'center',padding:spacing.md,backgroundColor:colors.surfaceElevated,borderRadius:radius.md,gap:3},label:{...typography.caption,color:colors.text},sleeve:{width:32,height:8,backgroundColor:colors.textMuted},plate:{minWidth:34,borderRadius:4,backgroundColor:colors.accent,alignItems:'center',justifyContent:'center'},text:{fontSize:10,fontWeight:'900',color:colors.accentText},collar:{...typography.caption,color:colors.textMuted,marginLeft:4}});
