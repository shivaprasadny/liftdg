import { StyleSheet, Text, View } from 'react-native'; import { WorkoutSetHistoryRow } from './WorkoutSetHistoryRow'; import { colors } from '@/constants/colors'; import { radius, spacing } from '@/constants/spacing'; import { typography } from '@/constants/typography'; import type { WorkoutExerciseDetails } from '@/types/workout';
export function WorkoutExerciseHistoryCard({ item }: { item: WorkoutExerciseDetails }) { return <View style={styles.card}><Text style={styles.name}>{item.exercise.name}</Text><Text style={styles.meta}>{item.exercise.category} · {item.exercise.equipment}</Text><Text style={styles.meta}>Target {item.targetSets ?? '—'} sets · {item.targetRepsMin ?? '—'}–{item.targetRepsMax ?? '—'} reps · Rest {item.restSeconds ?? 0}s</Text>{item.notes ? <Text style={styles.notes}>{item.notes}</Text> : null}{item.sets.map((set) => <WorkoutSetHistoryRow key={set.id} set={set} />)}</View>; }
const styles = StyleSheet.create({ card: { padding: spacing.lg, gap: spacing.sm, borderRadius: radius.lg, backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border }, name: { ...typography.heading, color: colors.text }, meta: { ...typography.caption, color: colors.textMuted }, notes: { ...typography.body, color: colors.text, fontStyle: 'italic' } });
