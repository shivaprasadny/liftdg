import{describe,expect,it}from'vitest';import{format}from'date-fns';import{plannerGridRange}from'@/services/scheduledWorkoutService';
describe('training month planner range',()=>{it('returns complete Monday-first weeks covering the month',()=>{const range=plannerGridRange(new Date(2026,6,1),1);expect(format(range.from,'yyyy-MM-dd')).toBe('2026-06-29');expect(format(range.to,'yyyy-MM-dd')).toBe('2026-08-02');});it('supports a Sunday-first preference',()=>{const range=plannerGridRange(new Date(2026,6,1),0);expect(format(range.from,'yyyy-MM-dd')).toBe('2026-06-28');expect(format(range.to,'yyyy-MM-dd')).toBe('2026-08-01');});});
