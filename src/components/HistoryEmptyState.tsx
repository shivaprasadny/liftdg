import { router } from 'expo-router'; import { StyleSheet, Text, View } from 'react-native';
import { AppButton } from './AppButton'; import { colors } from '@/constants/colors'; import { spacing } from '@/constants/spacing'; import { typography } from '@/constants/typography';
export function HistoryEmptyState({ mode }: { mode: 'none' | 'search' | 'filter' }) { const message = mode === 'none' ? 'Finish your first workout to see it here.' : mode === 'search' ? 'No workouts match your search.' : 'Try changing or clearing your filters.'; return <View style={styles.wrap}><Text style={styles.title}>No workouts found</Text><Text style={styles.text}>{message}</Text>{mode === 'none' ? <AppButton label="Start Workout" onPress={() => router.push('/start')} /> : null}</View>; }
const styles = StyleSheet.create({ wrap: { padding: spacing.xxl, alignItems: 'center', gap: spacing.md }, title: { ...typography.heading, color: colors.text }, text: { ...typography.body, color: colors.textMuted, textAlign: 'center' } });
