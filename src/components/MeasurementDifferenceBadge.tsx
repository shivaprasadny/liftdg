import{StyleSheet,Text,View}from'react-native';import{colors}from'@/constants/colors';import{radius,spacing}from'@/constants/spacing';import{typography}from'@/constants/typography';export function MeasurementDifferenceBadge({value,unit}:{value:number;unit:string}){const sign=value>0?'+':value<0?'−':'';const description=value>0?'increased':value<0?'decreased':'unchanged';return <View style={styles.badge} accessibilityLabel={`${description} by ${Math.abs(value).toFixed(1)} ${unit}`}><Text style={styles.text}>{sign}{Math.abs(value).toFixed(1)} {unit}</Text></View>}const styles=StyleSheet.create({badge:{paddingHorizontal:spacing.sm,paddingVertical:spacing.xs,borderRadius:radius.pill,backgroundColor:colors.surfaceElevated},text:{...typography.label,color:colors.text}});
