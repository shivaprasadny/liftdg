import { Pressable, StyleSheet, Text, View } from 'react-native';
import type { ExerciseRecommendation } from '@/types/exerciseReplacement';
import { colors } from '@/constants/colors';import{radius,spacing}from '@/constants/spacing';import{typography}from '@/constants/typography';
export function ReplacementExerciseCard({item,selected,onPress}:{item:ExerciseRecommendation;selected:boolean;onPress:()=>void}){return <Pressable accessibilityRole="button" accessibilityState={{selected}} accessibilityLabel={`${item.exercise.name}. ${item.reasons.join('. ')}`} onPress={onPress} style={[styles.card,selected&&styles.selected]}><View style={styles.row}><Text style={styles.name}>{item.exercise.name}</Text><Text style={styles.category}>{item.category}</Text></View><Text style={styles.meta}>{item.exercise.category} · {item.exercise.equipment} · {item.exercise.exerciseType}</Text>{item.reasons.map(reason=><Text key={reason} style={styles.reason}>✓ {reason}</Text>)}</Pressable>};
const styles=StyleSheet.create({card:{padding:spacing.md,gap:spacing.xs,borderRadius:radius.lg,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border},selected:{borderColor:colors.accent},row:{flexDirection:'row',justifyContent:'space-between',gap:spacing.sm},name:{...typography.heading,color:colors.text,flex:1},category:{...typography.caption,color:colors.accent},meta:{...typography.caption,color:colors.textMuted},reason:{...typography.caption,color:colors.text}});
