import { formatDistanceToNow } from 'date-fns'; import { Pressable, StyleSheet, Text, View } from 'react-native';
import { colors } from '@/constants/colors'; import { radius, spacing } from '@/constants/spacing'; import { typography } from '@/constants/typography'; import type { WorkoutPlan } from '@/types/workoutPlan';
export function WorkoutPlanCard({ plan, onPress }: { plan: WorkoutPlan; onPress: () => void }) { return <Pressable onPress={onPress} style={({ pressed }) => [styles.card, pressed && styles.pressed]}>
  <View style={[styles.bar, { backgroundColor: plan.color ?? colors.accent }]} /><View style={styles.content}><View style={styles.head}><Text style={styles.name}>{plan.name}</Text><Text style={styles.badge}>{plan.isBuiltin ? 'STARTER' : 'MY PLAN'}</Text></View>
  {plan.description ? <Text numberOfLines={2} style={styles.description}>{plan.description}</Text> : null}<Text style={styles.meta}>{plan.exerciseCount} exercises · {plan.estimatedSetCount} sets</Text><Text style={styles.updated}>Updated {formatDistanceToNow(new Date(plan.updatedAt), { addSuffix: true })}</Text></View></Pressable>; }
const styles = StyleSheet.create({ card: { flexDirection: 'row', backgroundColor: colors.surface, borderRadius: radius.lg, borderWidth: 1, borderColor: colors.border, overflow: 'hidden' }, pressed: { opacity: .72 }, bar: { width: 6 }, content: { flex: 1, padding: spacing.lg, gap: spacing.sm }, head: { flexDirection: 'row', justifyContent: 'space-between', gap: spacing.sm }, name: { ...typography.body, color: colors.text, fontWeight: '800', flex: 1 }, badge: { fontSize: 10, color: colors.accent, fontWeight: '800' }, description: { ...typography.caption, color: colors.textMuted }, meta: { ...typography.label, color: colors.text }, updated: { ...typography.caption, color: colors.textMuted } });
